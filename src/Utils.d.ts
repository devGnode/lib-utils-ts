export declare class Utils {
    static regExp(regexp: RegExp, value: string, callback: Function): string;
    /***
     * @param directory
     * @return {boolean}
     */
    static exists(directory?: string): Boolean;
}
